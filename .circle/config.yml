# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/configuration-reference
version: 2.1

general:
  build_dir: deliverables/D3

orbs:
  node: circleci/node@5.1.0

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/configuration-reference/#jobs
jobs:
  #This is the job to check that CircleCI is configured properly.
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo CircleCI properly configured."

#This is the job to check that all jest tests have passed.
#NOW REPLACED WITH ORB NODE/TEST in workflow
  # test:
  #   docker:
  #     - image: cimg/node:16.10
  #   steps:
  #     - checkout
  #     - node/install-packages:
  #         pkg-manager: npm
  #     - run:
  #         name: "Run Tests"
  #         command: npm test
  #     - store_test_results:
  #         path: test-results

  #This is the job to publish to npm.
  publish:
    docker:
      - image: cimg/node:16.10
    steps:
      - checkout
      - run:
          name: Publish to NPM
          command: |
            npm set //registry.npmjs.org/:_authToken=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
            npm publish

# Orchestrate jobs using workflows
# See: https://circleci.com/docs/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
  test-and-publish:
    jobs:
      - node/test:
          test-results-for: jest
      - publish:
          requires:
            - node/test